#ifndef CORE_H_
#define CORE_H_

// #include <cstdlib>

// extern "C" void elem_exp(double * out, const double * in, size_t len);
#include <stdlib.h>


void elem_exp(double * out, const double * in, size_t len);

#endif // CORE_H_
